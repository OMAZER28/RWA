To Create a Graph:
-Left clic-->add a new node
-Right clic-->select a node
-Select two nodes-->add an edge between them
-Select two nodes that have a common edge-->remove the edge between them
-Select a node + backspace key-->remove a node